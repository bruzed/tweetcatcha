package com.NickHardeman.utils {		public class Dimensional {				public function Dimensional() {			trace("Started the dimensional class");		}					public function scaleProportional(photoWidth:Number, photoHeight:Number, MaxWidth:Number, MaxHeight:Number):Object { 			//var StartWidth:Number = inObject._width;			//var StartHeight:Number = inObject._height;			var newHeight:Number = photoHeight;			var newWidth:Number = photoWidth;			var dimenObj:Object = new Object();			if (MaxWidth > 0 && MaxHeight > 0 ) {								if (photoWidth > MaxWidth) {					newWidth = MaxWidth;					newHeight = photoHeight * (MaxWidth/photoWidth);				}								photoWidth = newWidth;				photoHeight = newHeight;				if (photoHeight > MaxHeight) {					newHeight = MaxHeight;					newWidth = photoWidth * (MaxHeight/photoHeight);				}			}			dimenObj.fullWidth = newWidth;			dimenObj.fullHeight = newHeight;			return dimenObj;		}				public static function scaleProportionally(photoWidth:Number, photoHeight:Number, MaxWidth:Number, MaxHeight:Number):Object { 			//var StartWidth:Number = inObject._width;			//var StartHeight:Number = inObject._height;			var newHeight:Number = photoHeight;			var newWidth:Number = photoWidth;			var dimenObj:Object = new Object();			if (MaxWidth > 0 && MaxHeight > 0 ) {								if (photoWidth > MaxWidth) {					newWidth = MaxWidth;					newHeight = photoHeight * (MaxWidth/photoWidth);				}								photoWidth = newWidth;				photoHeight = newHeight;				if (photoHeight > MaxHeight) {					newHeight = MaxHeight;					newWidth = photoWidth * (MaxHeight/photoHeight);				}			}			dimenObj.fullWidth = newWidth;			dimenObj.fullHeight = newHeight;			return dimenObj;		}				public function traceIt():void {			trace("talkding to the dimen class");		}				public static function FitProportionally(photoWidth:Number, photoHeight:Number, MinWidth:Number, MinHeight:Number):Object { 			var newHeight:Number = photoHeight;			var newWidth:Number = photoWidth;			var dimenObj:Object = new Object();			if (MinWidth > 0 && MinHeight > 0 ) {								if (photoWidth < MinWidth) {					newWidth = MinWidth;					newHeight = photoHeight * (MinWidth/photoWidth);				}								photoWidth = newWidth;				photoHeight = newHeight;				if (photoHeight < MinHeight) {					newHeight = MinHeight;					newWidth = photoWidth * (MinHeight/photoHeight);				}			}			dimenObj.fullWidth = newWidth;			dimenObj.fullHeight = newHeight;			return dimenObj;		}				public static function FitObjectProp(startW:Number, startH:Number, MaxWidth:Number, MaxHeight:Number):Object {			var dimenObj:Object = new Object();			var newWidth:Number = startW;			var newHeight:Number = startH;						var StartWidth:Number = startW;			var StartHeight:Number = startH;			if (MaxWidth > 0 || MaxHeight > 0 ) {				if (StartWidth < MaxWidth) {					newWidth = MaxWidth;					newHeight = StartHeight * (MaxWidth/StartWidth);				}				StartWidth = newWidth;				StartHeight = newHeight;				if (StartWidth > MaxWidth && StartHeight > MaxHeight) {					newWidth = MaxWidth;					newHeight = StartHeight * (MaxWidth/StartWidth);				}				StartWidth = newWidth;				StartHeight = newHeight;				if (StartHeight < MaxHeight) {					newHeight = MaxHeight;					newWidth = StartWidth * (MaxHeight/StartHeight);				}				StartWidth = newWidth;				StartHeight = newHeight;				if (StartHeight > MaxHeight && StartWidth > MaxWidth) {					newHeight = MaxHeight;					newWidth = StartWidth * (MaxHeight/StartHeight);				}			}			dimenObj.fullWidth = newWidth;			dimenObj.fullHeight = newHeight;			return dimenObj;		}				public static function FitInsideProp(photoWidth:Number, photoHeight:Number, MinWidth:Number, MinHeight:Number):Object { 			var newHeight:Number = photoHeight;			var newWidth:Number = photoWidth;			var dimenObj:Object = new Object();			if (MinWidth > 0 && MinHeight > 0 ) {								if (photoWidth > MinWidth) {					newWidth = MinWidth;					newHeight = photoHeight * (MinWidth/photoWidth);				}								photoWidth = newWidth;				photoHeight = newHeight;				if (photoHeight > MinHeight) {					newHeight = MinHeight;					newWidth = photoWidth * (MinHeight/photoHeight);				}			}			dimenObj.fullWidth = newWidth;			dimenObj.fullHeight = newHeight;			return dimenObj;		}							}}